---
"pi-mono-review": minor
---

Add structured review findings with P0-P3 priority, confidence, title/body fields, scoped `report_finding` tool-call review generation, compact findings rendering, and legacy severity/JSON fallback support.
