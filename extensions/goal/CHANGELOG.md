# pi-mono-goal

## 0.2.0

### Minor Changes

- Add persistent `/goal` management with `get_goal`, `create_goal`, and `update_goal` tools.
- Add plans, blockers, budgets, progress/accounting updates, hidden steering, and bounded automatic continuation.
- Add goal status and widget UI backed by append-only session persistence.

## 0.1.0

### Minor Changes

- Add Codex-style `/goal` command, goal tools, hidden steering, bounded auto-continuation, accounting, and UI status/widget.
